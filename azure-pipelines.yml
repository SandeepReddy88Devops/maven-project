trigger:
- master

strategy:
  matrix:
    linux:
      operatingSystem: 'ubuntu14.04'
    windows:
      operatingSystem: 'windows-latest'
pool:
  vmImage: $(operatingSystem)

steps:
- script: echo Running on $(operatingSystem)
  displayName: 'Run a one-line script'